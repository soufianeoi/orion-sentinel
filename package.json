{
  "name": "orion-sentinel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:nlp\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:nlp": "cd nlp-service && uvicorn main:app --host 0.0.0.0 --port 5000 --reload",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../nlp-service && pip install -r requirements.txt && python -m spacy download en_core_web_sm",
    "start": "npm run dev",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "nlp": "cd nlp-service && uvicorn main:app --host 0.0.0.0 --port 5000 --reload"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}